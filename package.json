{
  "name": "typescript",
  "version": "1.0.0",
  "description": "tsc --init  // add tsconfing.json",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "lite-server"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "systemjs": "^0.21.5"
  },
  "devDependencies": {
    "lite-server": "^2.5.3"
  }
}
